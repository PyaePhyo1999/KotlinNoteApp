<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.firebase">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".activities.LoginActivity"
                android:label="@string/app_name"
                android:exported="true"
                android:theme="@style/Theme.NoteList">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activities.NoteListActivity"
                  android:exported="true"
                android:theme="@style/Theme.NoteList"/>

        <activity android:name=".activities.AddNoteActivity"
                android:exported="true"
                android:theme="@style/Theme.NoteList"/>

        <activity android:name=".activities.LoginProfileActivity"
                android:exported="true"
                android:theme="@style/Theme.NoteList"/>

        <activity android:name=".activities.NoteDetailActivity"
                android:exported="true"
                android:theme="@style/Theme.NoteList"/>
    </application>

</manifest>